/* 
        Question: Write a function that accepts a array of items a person wants on a sandwich. 
        The function should have one parameter that collects as many items as the function call provides, 
        and it should print a summary of the sandwich that is being ordered. 
        Call the function three times, using a different number of arguments each time.

*/

function Sandwich(item) {
  item = sandwich;
  count = 0;
  while (count < 10) {
    item.push(sandwich[item]);
  }
}
let sandwich = [];

Sandwich("cheese");

Sandwich((items = ["honey dijon", "roast beef", "extracheddar cheese'"]));

Sandwich((items = ["peanut butter", "strawberry jam"]));
